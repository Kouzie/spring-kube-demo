# config map
apiVersion: v1
kind: ConfigMap
metadata:
  name: demo-config
  namespace: spring

data:
  application.properties: |-
    demo: "Hello Demo"
    calc.test: "Hello Calc"
    TEST_STRING: "Hello Test"
    TRACING_URL: http://localhost:4317
